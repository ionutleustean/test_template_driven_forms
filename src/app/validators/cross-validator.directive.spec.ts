import { CrossValidatorDirective } from './cross-validator.directive';

describe('CrossValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CrossValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
